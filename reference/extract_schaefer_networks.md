# Extract network names from Schaefer ROI labels

Extract network names from Schaefer ROI labels

## Usage

``` r
extract_schaefer_networks(roi_names)
```

## Arguments

- roi_names:

  Character vector of Schaefer ROI names

## Value

Character vector of network names (formatted for readability)
